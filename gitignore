# ---------------------------------------------------------------------
# Secrets and local wallets — NEVER commit these.
.env
.env.local
*.key
# ---------------------------------------------------------------------
id.json
*_keypair.json
agent_keypair.json
!target/deploy/tap_a2a-keypair.json   # program identity — see README

# ---------------------------------------------------------------------
# Build output
# ---------------------------------------------------------------------
target/
!target/deploy/
target/deploy/*
!target/deploy/tap_a2a-keypair.json
.anchor/
node_modules/
dist/

# ---------------------------------------------------------------------
# Local validator state and logs
# ---------------------------------------------------------------------
test-ledger/
validator.log
*.log

# ---------------------------------------------------------------------
# Python
# ---------------------------------------------------------------------
venv/
__pycache__/
*.pyc
.pytest_cache/

# ---------------------------------------------------------------------
# Formal verification scratch
# ---------------------------------------------------------------------
ablation_*.spthy
try.spthy
*.spthy.tmp

# ---------------------------------------------------------------------
# Benchmark artefacts regenerated by the pipeline.
# The committed copies live in dissertation_results/ instead, so the
# repo carries evidence even when nobody runs the suite.
# ---------------------------------------------------------------------
gateway_*_iterations.csv
figure_6_*.png
metrics_*.csv
metrics_*.txt

# ---------------------------------------------------------------------
# Editor / OS
# ---------------------------------------------------------------------
.vscode/
.idea/
.DS_Store
