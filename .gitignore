.env
.env.local
*.key
# Secrets and local wallets — NEVER commit these.
id.json
*_keypair.json
agent_keypair.json

# Build output (keep only the fixed program identity)
target/*
!target/deploy/
target/deploy/*
!target/deploy/tap_a2a-keypair.json
.anchor/
node_modules/
dist/

# Local validator state and logs
test-ledger/
validator.log
*.log

# Python
venv/
__pycache__/
*.pyc
.pytest_cache/

# Stray tool installs
1.12.0/
scyther-main/

# Formal verification scratch
ablation_*.spthy
try.spthy

# Benchmark artefacts regenerated by the pipeline
# (committed copies live in dissertation_results/)
gateway_*_iterations.csv
metrics_*.csv
metrics_*.txt

# Editor / OS
.vscode/
.idea/
.DS_Store

# Working copies at the repo root; the committed copies live in
# dissertation_results/ so there is a single source of truth.
figure_6_*.png
gateway_*.csv
trs_ring_size.csv
tamarin_results.txt
